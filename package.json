{
  "name": "keygraphjs",
  "version": "0.3.0",
  "private": true,
  "description": "A small, dependency-free web editor for undirected, edge-weighted graphs.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
